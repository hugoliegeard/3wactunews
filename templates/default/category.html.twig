{% extends "base.html.twig" %}

{% block title %}
    {{ app.request.get('aliasDeMaCategorie')|capitalize }}
{% endblock %}

{% block body %}

    {#{{ dump(app.request) }}#}

    <main>
        <!-- Titre de la page -->
        <div class="p-3 mx-auto text-center">
            <h1 class="display-4">
                {{ app.request.get('aliasDeMaCategorie')|capitalize }}
            </h1>
        </div>

        <!-- Contenu de la page -->
        <div class="py-5 bg-light">
            <div class="container">
                <div class="row">
                    {% for i in 0..5 %}
                        {% include 'partials/_card.html.twig' %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </main>
{% endblock %}
